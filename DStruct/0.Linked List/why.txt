顺序表的实现中为什么保存的是具体数据元素的地址？
而保存的地址存放在unsigned int中，而不是具体的指针类型中？
为什么不是void*呢？


以下关于链式存储结构的叙述中正确的是
A. 链式存储结构不是顺序存取结构
B. 逻辑上相邻的结点物理必相邻
C. 可以通过计算直接确定第i个元素
D. 插入和删除操作方便，不必移动其它结点

无表头结点的单链表。编程实现这种无表头结点的单链表
并对比表头实现方式和无表头实现方式哪种更好。

顺序表和单链表添加一个反转操作
要求时间复杂度达到O(n)

SeqList_Reverse(SeqList* list);
LinkList_Reverse(LinkList* list);